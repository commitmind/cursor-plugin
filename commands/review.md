---
description: Grounded AI review of your pending changes (deterministic + judgment, metered)
---
Review my pending changes using the `review_ai` MCP tool (scope: working).

Then present the findings grouped by severity (critical/high first), each as one line: what's wrong and where. For every high or critical finding, offer the next step — fix it with `propose_fix` (preview the diff first) or, if it's a recurring class of mistake, capture it as a rule with `draft_rule`.

Do NOT apply any fix or record any rule without my explicit go-ahead.
